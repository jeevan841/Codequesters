# Codequesters — Voice CRM Backend Dependencies
# Generated: 2026-06-21
# Only packages actually imported by app.py, create_db.py, and tests/.
# Removed: torch, transformers, matplotlib, gradio, pandas, openai,
#          google-generativeai, mcp, fastmcp, pywin32 (Windows-only, breaks Docker),
#          Flask, grpcio, huggingface_hub, numpy, twilio, and all other unused packages.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
starlette==0.41.3
python-multipart==0.0.18

# Database
psycopg2-binary==2.9.10

# Auth & sessions
passlib[bcrypt]==1.7.4
PyJWT==2.10.1
authlib==1.4.0
itsdangerous==2.2.0
cryptography==44.0.0

# Rate limiting
slowapi==0.1.9

# LLM clients
groq==0.13.0
ollama==0.4.7

# Env & config
python-dotenv==1.0.1

# HTTP client (OAuth + Exotel calls)
requests==2.32.3
httpx==0.27.2

# Data validation
pydantic==2.10.3
pydantic-settings==2.7.0
annotated-types==0.7.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
